<header toolbar>
    <button save class="button is-primary">Save</button>
    <button new class="button">New</button>

    <div class="select">
        <select theme>
            {{#each editor.themes }}
                <option value="{{ this.id }}">{{ this.name }}</option>
            {{/each}}
        </select>
    </div>
</header>
